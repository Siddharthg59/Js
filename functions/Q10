function identityMatrix(n) {
  for (let i = 0; i < n; i++) {
    let row = '';
    for (let j = 0; j < n; j++) {
      row += (i === j ? '1' : '0') + ' ';
    }
    console.log(row);
  }
}

identityMatrix(4);
