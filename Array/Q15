let choices = ["Red", "Green", "Blue", "Yellow"];
for (let i = 0; i < choices.length; i++) {
  let suffix = (i + 1 === 1) ? "st" :
               (i + 1 === 2) ? "nd" :
               (i + 1 === 3) ? "rd" : "th";
  console.log(`${i + 1}${suffix} choice is ${choices[i]}`);
}
