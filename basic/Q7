for (let year = 2014; year <= 2050; year++) { // Loop through years 2014 to 2050.
  const date = new Date(year, 0, 1); // Create a date for Jan 1st of the current year.
  if (date.getDay() === 0) { // 0 = Sunday
    console.log(`1st January is a Sunday in ${year}`); // Print the year.
  }
}
