function findLarger(a, b) {
  if (a > b) {
    console.log(`${a} is larger`);
  } else if (b > a) {
    console.log(`${b} is larger`);
  } else {
    console.log("Both are equal");
  }
}

findLarger(10, 20); // 20 is larger
