function isEven(n) {
  n = Math.abs(n);                        // Handle negative numbers
  if (n === 0) return true;
  if (n === 1) return false;
  return isEven(n - 2);
}

console.log(isEven(10)); // true
