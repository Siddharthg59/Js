function range(x, y) {
  if (x + 1 >= y) return [];                 // Base case: no numbers in range
  return [x + 1, ...range(x + 1, y)];        // Recursive case
}

console.log(range(2, 9)); // [3, 4, 5, 6, 7, 8]
